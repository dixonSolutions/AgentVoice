/**
 * Re-export of the shared matcher. The implementation moved to
 * @agentvoice/client so the editor uses the same rules; this file stays so the
 * app's existing imports keep working.
 */
export * from '@agentvoice/client';
